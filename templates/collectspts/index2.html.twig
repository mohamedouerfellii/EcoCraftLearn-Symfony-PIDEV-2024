{% extends 'admin.html.twig' %}

{% block body %}
<div class="content-page">
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Liste des collect points </h5>

                            <!-- Formulaire de recherche -->
                            <form action="{{ path('afficher_collectspts2') }}" method="GET">
                                <div class="form-group">
                                    <label for="search">Rechercher par  Description :</label>
                                    <input type="text" class="form-control" id="search" name="search" value="{{ app.request.query.get('search') }}">
                                </div>
                                <button type="submit" class="btn btn-primary">Rechercher</button>
                            </form>
                            <!-- Fin du formulaire de recherche -->

                            <!-- Tableau des collectsptss filtrées -->
                            {% for recl in Collectspts %}
                            <div class="card mb-3">
                                <div class="card-body">
                                    <h5 class="card-title">Nom: {{ recl.name }}</h5>
                                    
                                    
                                    
                                    <p class="card-text">Description: {{ recl.adresse }}</p>
                                    <p class="card-text">capacité: {{ recl.capacity }}</p>
                                    
                                    <img src="{{ asset('uploads/images/' ~ recl.image) }}" style="width:80px; height:80px;" />
                                    <div class="btn-group" role="group" aria-label="Actions">
                                        
                                        <a class="btn cur-p btn-danger" href="{{ path('delete_av', {'id': recl.id}) }}">Supprimer</a>
                                        <a class="btn cur-p btn-success" href="{{ path('update_av', {'id': recl.id}) }}">Mettre à jour</a>
                                    </div>
                                </div>
                            </div>
                            {% endfor %}
                            <!-- Fin du tableau des collectsptss filtrées -->

                            <!-- Pagination -->
                            <div class="pagination">
                                {% set currentPage = Collectspts.currentPageNumber %}
                                {% set previous = currentPage > 1 ? currentPage - 1 : false %}
                                {% set next = currentPage < Collectspts.pageCount ? currentPage + 1 : false %}
                                <ul class="pagination">
                                    {% if previous %}
                                        <li class="page-item">
                                            <a class="page-link" href="{{ path('afficher_collectspts2') }}?page={{ previous }}">Précédent</a>
                                        </li>
                                    {% endif %}
                                    <li class="page-item active">
                                        <span class="page-link">{{ currentPage }}</span>
                                    </li>
                                    {% if next %}
                                        <li class="page-item">
                                            <a class="page-link" href="{{ path('afficher_collectspts2') }}?page={{ next }}">Suivant</a>
                                        </li>
                                    {% endif %}
                                </ul>
                            </div>
                            <!-- Fin de la pagination -->
                            <!-- Ajoutez ce lien à la suite de votre contenu -->
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
